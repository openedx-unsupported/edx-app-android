<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:ta="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="org.edx.mobile.tta.ui.course.view_model.CourseDashboardViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/app_background">

        <android.support.v7.widget.Toolbar
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:id="@+id/toolbar"
            android:background="@color/white"
            app:title="@{viewModel.course.name}"
            app:navigationIcon="@drawable/back"
            app:titleTextAppearance="@style/ta_toolbar_title_style"
            android:gravity="center_vertical"/>

        <android.support.design.widget.TabLayout
            android:id="@+id/tab_layout"
            style="@style/ta_tab_style"
            app:tabGravity="fill"
            app:tabIndicatorColor="@color/primary_cyan"
            app:tabIndicatorHeight="2dp"
            app:tabSelectedTextColor="@color/primary_cyan"
            app:tabTextColor="@color/primary_navy"
            android:layout_marginTop="1dp"/>

        <android.support.v4.view.ViewPager
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/view_pager"
            ta:pager_adapter="@{viewModel.adapter}"/>

    </LinearLayout>

</layout>